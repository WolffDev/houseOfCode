// Arguments passed into this controller can be accessed via the `$.args` object directly or:
// var args = $.args;
thisWindow = $.create;

function createEvent(e){
    var eventTitle = $.createTitle.value;
    alert(loggedInUser);
}
